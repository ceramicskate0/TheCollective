#!/usr/bin/phython

import socket

ip= input("Enter IP:")
portNum=port("Enter Port Number:")
s=socket.socket(socket.AF_INET,socket.SOCK_STREAM)
if s.connect_ex((ip,portNum)):
  print "Port " , portNum,"is closed"
else
print "Port ",portNum, "is open"
